#ifndef ADT_BST_TREE_MENU_H
#define ADT_BST_TREE_MENU_H
#include "BST.h"
#include "test.h"
#include <ctime>

void textMenu();

void forwardIteratorMenu(BST<int, int> &tree);

void reverseIteratorMenu(BST<int, int> &tree);

void insertMenu(BST<int, int> &tree);

void menu();

#endif //ADT_BST_TREE_MENU_H
